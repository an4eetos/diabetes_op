# Cloudflare Workers (static assets + SPA).
# Deploy from frontend/: npx wrangler deploy
# Set `name` to match your Worker in the dashboard if different.
name = "diabetes"
compatibility_date = "2026-05-16"

[assets]
directory = "./dist"
not_found_handling = "single-page-application"
